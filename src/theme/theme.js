import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: "Rubik, sans-serif",
  },
  palette: {
    background: {
      default: "#e0e0e0",
    },
    primary: {
      main: "#1a2327",
    },
    secondary: {
      main: "#30475E",
    },
    black: {
      main: "#1a2327",
    }
  },
});
